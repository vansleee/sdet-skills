# Structured Result

把測試/觀察的結果，從「Pass/Fail」二分，擴充成六種狀態，每筆附證據。

## 設計理念（為什麼這樣設計）

- **「Fail」扛太多意思。** 環境掛了、時好時壞、證據不足、真 bug——二分法把這些全塞進 Fail，等於什麼都沒說；判斷責任又丟回給人。
- **拆開「非 Pass」才報得準。** `blocked`（測不到判斷點）、`inconclusive`（證據不足）、`anomaly`（範圍外可疑）、`flaky`（不穩定）各自獨立，一眼就知道哪個要現在處理。
- **錯誤情境被正確擋下 = pass。** 狀態對應的是「符不符合預期」，不是「畫面上有沒有紅字」。
- **承認不知道是專業。** 誠實的 `blocked` / `inconclusive` 比硬給一個你不敢信的 Pass/Fail 有用。
- **這是 False Positive 的第一道閘。** 只有 `fail`（及經分類確認的 `anomaly`）能往下開單；`blocked` / `inconclusive` 不得直接變 Issue（Day 23）。

## 承先啟後
搭配 `evidence-package`（證據長怎樣）；是 Day 13 分類、Day 20 Test Oracle、Day 22 Confidence、Day 26 Bug Verifier 的共同詞彙。對應文章：Day 12。
