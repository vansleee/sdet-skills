# Classify Anomaly

看到異常，先分類是哪一種原因，再決定是否往下——而不是每看到紅字就當 bug。

## 設計理念（為什麼這樣設計）

- **一個異常有很多種原因，只有一種是產品 bug。** 環境、測資、agent 自己操作錯、間歇 flaky、已知問題——不分類就報，等於開一間誤報工廠。
- **依證據判類，不靠猜。** 每次判類都要指出依據哪條 evidence（network/console/截圖）。
- **跨 finding 檢查。** 多筆共用同一錯誤簽章，通常是 environment 或 stack-wide 事件，歸一類，不要逐筆當產品 bug。（沿用 CI triage 的 infra 合併經驗。）
- **只有 product-bug 能往下。** 其餘不直接變 Issue——這是控制 False Positive 的源頭（Day 23）。

## 承先啟後
上游：`structured-result`（fail / anomaly）。下游：Day 20 Test Oracle（更嚴謹判定符不符預期）、Day 22 Confidence、Day 26 Verifier、Day 23 False Positive、第四週 `triage`。對應文章：Day 13。
