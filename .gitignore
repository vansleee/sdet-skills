node_modules/
.env
.env.*
config/product-context.md
config/sdet-config.yaml
config/governance.yaml
config/test-style.md
# 多專案佈局：config/<project>/ 底下全是真檔（含 base URL、帳密變數名），整個目錄不進版控
config/*/
test-results/
playwright-report/
.pw-mcp-traces/
*.zip
.DS_Store
.playwright-mcp/
.playwright-cli/
.fuse_hidden*

# 產品知識：只 commit 範本（*.example.md）與 README，真實產品事實留本地
knowledge/**
!knowledge/**/
!knowledge/README.md
!knowledge/**/*.example.md

# 所有執行期產物與狀態檔都集中在 output/（2026-07-30 起），整個 gitignore。
# 只 commit state-templates/ 下的 *.example.yaml 範本。真檔（evidence、verdicts、issues-index、
# calibration、gate、runs、reports…）全留本地。
# Playwright MCP 的 trace/snapshot 暫存也走 output/evidence/_traces/（見 README 的 MCP 設定）。
/output/

# 保險：舊版曾把產物落在 repo 根目錄，留著擋回頭路
/evidence/

# Claude Code 個人設定(不進版控;團隊共用的規則放 .claude/settings.json)
.claude/settings.local.json*

# Ulysses 外部資料夾的個人狀態，不進版控
book/.Ulysses-*.plist

# Python 快取
__pycache__/
*.pyc
