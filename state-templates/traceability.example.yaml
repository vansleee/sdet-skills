# 需求 ↔ 測試 ↔ finding 覆蓋對照（範本 — 複製成 traceability.yaml；真檔已 gitignore）
#
# 寫它的 skill：traceability。讀它的：test-planning（gap 排下輪）、
# release-signoff（放行證據）、pipeline-observability（coverage_gap 指標）。
#
# 對應規則（req_id 命名、測試怎麼宣告覆蓋、信心判定）見 references/traceability-mapping.md，
# 不放這裡。狀態檔只存資料，不存演算法。
#
# 限制：本表只回答「有沒有東西在守」，不回答「守得夠不夠」。
#      uncertain 一律不計入 covered。刻意不算單一覆蓋率百分比。

generated_at: 2026-07-29

requirements:
  - req_id: REQ-CHECKOUT-005
    source: "knowledge/domains/checkout.md#折扣碼"
    statement: "過期折扣碼不得套用，並顯示明確錯誤"
    covered_by:
      tests:
        # level：api | ui，由測試檔位置判（判準見 references/traceability-mapping.md）
        - { nodeid: "tests/api/checkout-coupon.spec.ts > rejects expired coupon", level: api }
        - { nodeid: "tests/checkout.spec.ts > shows discount line", level: ui }
      findings: ["F-2026-07-24-003"]
    confidence: high           # high（測試自己宣告）| low（檔名語意推測 → uncertain）
    status: covered            # covered | gap | uncertain | retired

  - req_id: REQ-CHECKOUT-006
    source: "knowledge/domains/checkout.md#折扣碼"
    statement: "折扣碼與會員點數不得同時使用"
    covered_by: { tests: [], findings: [] }
    status: gap
    risk_score: 0.71           # 由 route-by-risk 給，用來排補洞順序

  - req_id: REQ-AUTH-012
    source: "knowledge/domains/auth.md#SSO"
    statement: "SSO 登入失敗需退回本地登入並保留原始導向目標"
    covered_by:
      tests:
        - { nodeid: "tests/login.spec.ts > sso redirect", level: ui }
      findings: []
    confidence: low            # 只有檔名語意相符，非測試自己宣告
    status: uncertain
    note: "建議把 REQ-AUTH-012 加成 test annotation/tag，升成 high"

  - req_id: REQ-CART-003
    source: "knowledge/domains/cart.md#匿名購物車"
    statement: "（已下線）匿名購物車保留 30 天"
    status: retired            # id 保留供歷史追溯，不回收再利用

gaps:
  - { req_id: REQ-CHECKOUT-006, risk_score: 0.71, suggestion: "交 test-planning 排進下輪" }

# 有東西在守，但守在比較貴、比較脆的那一層。status 仍是 covered，故意跟 gaps 分開列。
level_mismatches:
  - req_id: REQ-CHECKOUT-009
    covered_at: [ui]
    should_be: api
    risk_score: 0.55
    suggestion: "折扣疊加規則的 4 個分支都用 UI 測試在守；降到 API 層交 api-test-author，原本那批交 test-prune 評估"

orphans:
  tests:
    - nodeid: "tests/legacy-promo.spec.ts > applies promo banner"
      note: "對不到任何 req；先確認 knowledge/ 是否漏寫此需求，再談 test-prune"
      route: test-prune
  findings:
    - id: "F-2026-07-26-011"
      note: "折扣碼大小寫不敏感，knowledge/ 未記載，可能是隱性需求"
      route: "補 knowledge/"

caveat: "只回答『有沒有東西在守』，不回答『守得夠不夠』；uncertain 1 筆未計入 covered；level_mismatches 1 筆仍算 covered。"
